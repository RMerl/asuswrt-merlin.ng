all:
	@cp prebuilt/vis-datacollector vis-datacollector

install:
	@install -D vis-datacollector ../installbin/bin/vis-datacollector

clean:
	@rm ../installbin/bin/vis-datacollector

install-strip: install
