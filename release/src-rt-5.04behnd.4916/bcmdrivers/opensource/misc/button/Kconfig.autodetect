config BCM_BUTTON
	bool "Broadcom BCA Board Button driver"
	depends on BCM_BCA_EXTINTR
	default n
	help
	  Add the BCM BCA External Interrup support.

config BCM_BUTTON_IMPL
	int "Implementation index for BCA Board Button module"
	depends on BCM_BUTTON
	default 1
