if TARGET_DRACO

config SYS_BOARD
	default "draco"

config SYS_VENDOR
	default "siemens"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "draco"

endif

if TARGET_THUBAN

config SYS_BOARD
	default "draco"

config SYS_VENDOR
	default "siemens"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "thuban"

endif

if TARGET_RASTABAN

config SYS_BOARD
	default "draco"

config SYS_VENDOR
	default "siemens"

config SYS_SOC
	default "am33xx"

config SYS_CONFIG_NAME
	default "rastaban"

endif

if TARGET_ETAMIN

config SYS_BOARD
        default "draco"

config SYS_VENDOR
        default "siemens"

config SYS_SOC
        default "am33xx"

config SYS_CONFIG_NAME
        default "etamin"

endif
