# vorbis uninstalled pkg-config file

prefix=
exec_prefix=
libdir=${pcfiledir}/lib
includedir=${pcfiledir}/include

Name: vorbis
Description: vorbis is the primary Ogg Vorbis library (uninstalled)
Version: 1.2.3
Requires: ogg
Conflicts:
Libs: -lm ${libdir}/libvorbis.la
Cflags: -I${includedir}
