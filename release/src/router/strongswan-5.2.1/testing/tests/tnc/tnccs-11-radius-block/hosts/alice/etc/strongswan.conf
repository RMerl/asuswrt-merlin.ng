# /etc/strongswan.conf - strongSwan configuration file

libimcv {
  debug_level = 3
  assessment_result = no
  plugins {
    imv-scanner {
      closed_port_policy = no
      tcp_ports = 80 443
     }
  }
}
