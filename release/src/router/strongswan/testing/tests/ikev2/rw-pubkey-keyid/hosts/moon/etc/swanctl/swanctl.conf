connections {

   rw-carol {
      include swanctl_base.conf

      remote {
         auth = pubkey
         pubkeys = carolPub.pem
      }
   }

   rw-dave {
      include swanctl_base.conf

      remote {
         auth = pubkey
         pubkeys = davePub.pem
      }
   }
}
